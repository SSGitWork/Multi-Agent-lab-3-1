[pytest]
asyncio_mode = auto
testpaths = tests
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning:mcp.*
markers =
    llm: marks tests that make live LLM calls (deselected by default)
